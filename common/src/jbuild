(library
 ((name incr_dom_widgets_common)
  (public_name incr_dom_widgets.common)
  (preprocess (pps (ppx_jane -ite-check ppxlib.runner)))
  (libraries (
    core_kernel
    ))
  ))


(jbuild_version 1)
